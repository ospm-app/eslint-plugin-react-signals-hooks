export function getRuleDocUrl(ruleName: string): string {
  // Keep docs path consistent with repository layout
  return `https://github.com/ospm-app/eslint-plugin-react-signals-hooks/tree/main/packages/eslint-plugin-valibot/docs/rules/${ruleName}.md`;
}
